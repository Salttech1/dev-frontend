import { InptfocsDirective } from './inptfocs.directive';

describe('InptfocsDirective', () => {
  it('should create an instance', () => {
    const directive = new InptfocsDirective();
    expect(directive).toBeTruthy();
  });
});
