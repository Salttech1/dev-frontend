import { YearmonthformatdirectiveDirective } from './yearmonthformatdirective.directive';

describe('YearmonthformatdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new YearmonthformatdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
