import { ElementsearchPipe } from './elementsearch.pipe';

describe('ElementsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ElementsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
